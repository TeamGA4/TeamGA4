#include "Header.h"

//アクション
void CharaSelect::Action()
{

}

//ドロー
void CharaSelect::Draw()
{
	imgchara = LoadGraph("使うキャラ\\カオスドラゴン.png");//キャラクター読み込み

	imgchara = LoadGraph("使うキャラ\\シャーリー.png");//キャラクター読み込み
	
	imgchara = LoadGraph("使うキャラ\\ブラン.png");//キャラクター読み込み
	
	imgchara = LoadGraph("使うキャラ\\ホーリーワイバーン.png");//キャラクター読み込み
	
	imgchara = LoadGraph("使うキャラ\\響　音符.png");//キャラクター読み込み
	
	imgchara = LoadGraph("使うキャラ\\空音　奏.png");//キャラクター読み込み
	
	imgchara = LoadGraph("使うキャラ\\神歌　姫.png");//キャラクター読み込み
	
	imgchara = LoadGraph("使うキャラ\\氷雨.png");//キャラクター読み込み
	
	imgchara = LoadGraph("使うキャラ\\野村　航平.png");//キャラクター読み込み

}
